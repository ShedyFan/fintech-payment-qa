# Сессия — аудит соответствия требованиям курса перед сдачей

- **Дата:** 2026-08-29
- **Ассистент:** Claude Code (Agent SDK), основная сессия — модель
  `claude-sonnet-5`. Для кросс-проверки другой моделью (слайд 26) запущены
  субагенты: один на **Claude Opus** (независимый аудит — выполнен) и один на
  **Fable 5** (упал: `Fable 5 requires usage credits`); Codex в окружении
  недоступен.
- **Режим разрешений:** обычные подтверждения (не `bypassPermissions`), без
  `/sandbox` (правки файлов на стороне Windows, песочница не включалась). В
  окружении активен хук `rtk` (переписывает часть bash-команд, местами ломает
  их — `rtk: command not found`); git-инспекция велась через PowerShell.
- **Статус:** завершена

## Промпты

Дословно, по порядку. Промпт 1 — большой инструктивный текст; воспроизведён с
сохранением структуры, служебные списки источников/задач сжаты до сути в местах,
помеченных `[…]` (это инструкция аудита, не постановка на изменение продукта).

### Промпт 1

> Ты работаешь с моим домашним заданием №2 курса AICOURSE (тема — QA-репозиторий
> для финтех-проекта). Моя цель — максимальный балл. Проверь репозиторий на
> соответствие ВСЕМ требованиям и найди / устрани всё, что может снизить оценку.
>
> Источники требований (прочитать все три, не полагаться на пересказ):
> `04 · Домашнее задание №2.md` (три обязательных пункта, чек-лист сдачи,
> критерии), `05 · Материалы к ДЗ №2.md` (промпты, приёмы, чек-лист раздела 11),
> `03 · Презентация по занятию 2.pdf` — с акцентом на слайды: 32 (финальный
> чек-лист «Обязательно / В плюс» с дедлайном), 20 (замер параллельно/
> последовательно: 128 тыс. токенов на трёх субагентах против ≈1 млн; по времени
> последовательно быстрее), 7–8 (`deny`/`ask`/`allow`; `bypassPermissions` не
> отключает `deny`; `deny` — шаблон на строку команды), 10–11 (песочница
> накрывает только Bash и дочерние процессы, не MCP), 5 («молча не выполнила» vs
> «сказала вслух»), 26 (кросс-проверка важных выводов — минимум три независимые
> модели: Opus, Codex, третья), 27 (перед сдачей проверить `.env`,
> `settings.json`, историю команд, скриншоты — секретов быть не должно); общий
> тон — честность и зафиксированные тупики повышают оценку.
>
> Репозиторий — локальный клон `F:\AI\fintech-payment-qa`, remote на
> `https://github.com/ShedyFan/fintech-payment-qa`, ветка `main`. Сначала
> `git fetch` и убедиться, что локальный `main` = `origin/main`. Правила
> проекта — `AGENTS.md`/`CLAUDE.md`, прочитать целиком (нельзя трогать код
> `app/` в режиме тестирования, формат session-файлов, формат коммитов на
> русском).
>
> Что нужно сделать: [1] пройтись по трём обязательным пунктам — каждый закрыт
> доказательствами, а не декларацией; [2] построчно по чек-листу «Что сдавать»
> (7 пунктов) и по чек-листу раздела 11; [3] по «Как оцениваем» и плюсовым
> пунктам — что сделано, что усилить дёшево; [4] проверка на секреты (`.env`,
> `settings.json`, история промптов в `sessions/`, скриншоты); [5] внутренняя
> согласованность README/REPORT/STATE/TOOLS между собой и с git; [6] актуальность
> PDF (`docs/test-plan.pdf`, `docs/report.pdf`), при устаревании и критичности —
> пересобрать `python docs/build-pdf.py` (с учётом ограничений `AGENTS.md` по
> хардкоду Chrome); [7] быстрые безопасные правки — сразу, коммит в стиле проекта
> + push; рискованное — отдельным пунктом с рекомендацией; [8] при содержательных
> правках — новый session-файл.
>
> Результат: сводный отчёт — (а) статус по каждому обязательному пункту и пункту
> чек-листа (закрыт / частично / не закрыт, с обоснованием и ссылкой на файл);
> (б) что доисправлено; (в) что осталось риском и что сделать вручную. Дедлайн —
> сегодня, 29 августа, 18:00.

### Промпт 2

> запускай правки, даю разрешение

### Промпт 3

> запускай правки осознанных рисков, даю разрешение

## Размышления

### Что проверялось и чем

- Три источника требований прочитаны целиком (`04 · Домашнее задание №2.md`,
  `05 · Материалы к ДЗ №2.md`; презентация — по пересказу ключевых слайдов в
  промпте, PDF в этой сессии не открывался).
- Git: `git fetch` + сверка `HEAD` с `origin/main` — совпадают (0/0), рабочее
  дерево чистое. История из 30+ коммитов разобрана по темам.
- Прочитаны: `AGENTS.md`, `CLAUDE.md`, `README.md`, `REPORT.md`, `SPEC.md`,
  `test-plan.md` (ID-диапазоны), `workflows/qa-regression-workflow.md`,
  `sessions/STATE.md`, `sessions/TOOLS.md`, все 9 session-файлов,
  `docs/build-pdf.py`, `test-areas/sample-transaction.json`.
- Скан секретов: `git grep` по `api_key|secret|token|password|PRIVATE KEY|sk-…|
  ghp_|xox…|AKIA…` + проверка `.gitignore` и `git ls-files`.

### Находки

1. **Обязательные пункты закрыты по содержанию.**
   - *Работающий результат* — `test-plan.md` (TR-044/CRD-044/FEE-048/APP-014/SK-55),
     `app/` как система под тестом, `README.md` с разделом «Как запустить». В
     `HEAD:test-plan.md` заглушек нет — `REPORT.md` § 1 «Оговорка по истории»
     описывала уже устаревшее состояние («коммитом не зафиксирована»), хотя
     сведение закоммичено в `d27e291` / `a542878`. → правка (см. ниже).
   - *Агенты параллельно* — реально сделано: `sessions/2026-08-29-parallel-bug-analysis.md`
     (три субагента одним батчем, замер времени 253 с < 393 с суммы) +
     `sessions/2026-08-29-benchmark-and-tools.md` (ещё три). Противоречие
     README↔REPORT про «параллельных агентов» уже снято в обоих файлах.
   - *Workflow* — `workflows/qa-regression-workflow.md`, два прогона в `sessions/`.

2. **Печатные PDF отстали от Markdown — пересобраны в этой же сессии.**
   `df3a3de` (2026-08-28 13:00) собрал обе версии; после этого в `test-plan.md`
   добавлена Область 4 (`a542878`, 18:42), а в `REPORT.md` — §§ 2 (обновление),
   7, 8, 9, 10 и поздние тупики. Сначала пересборку вынес автору (на машине нет
   системного Python 3, только Store-заглушка; `docs/build-pdf.py` хардкодил
   `C:\Program Files\Google\Chrome\...` при Chrome в `Program Files (x86)` и
   временную папку под фиксированным пользователем). **После явного разрешения автора** («запускай
   правки»): `build-pdf.py` сделан переносимым (поиск Chromium по списку
   кандидатов + `CHROME_BIN`, временная папка через `tempfile.gettempdir()`),
   `DATE_HUMAN` → 29 августа, строки сводки отчёта (пп. 1, 2, 3, 7, 8, 9, 10 и
   «стенд»→«Область 4 исполняемая») приведены к текущему `REPORT.md`. Рендер —
   Python 3.13.15 из кэша `uv` (`C:\Users\<user>\AppData\Roaming\uv\python\...`),
   headless Chrome. Оба PDF собрались (exit 0, `%PDF-`, 752 КБ / 554 КБ против
   653 КБ / 402 КБ), в `report.html` присутствуют все 10 h2-заголовков, в
   `test-plan.html` — `APP-001`/`APP-014`/`Область 4`/`TR-044`.

3. **Счёт регрессии 9/1/3 в `workflows/qa-regression-workflow.md` устарел** —
   после дополнения по аудиту (APP-008 переписан, добавлен APP-014) актуально
   9/2/3 из 14, как и в `STATE.md` и session-файле. → правка.

4. **Нет отдельного session-файла** на доустановку `rtk`/`claude-mem` и починку
   бага хука (`30f1c1d`, `0ddb0dc`). Работа отражена в `REPORT.md` § 7 и
   `TOOLS.md`, но `AGENTS.md` просит файл на сессию. → зафиксировано в `STATE.md`
   как известный пробел, ретроспективный файл не сочинял.

5. **Секретов нет.** `.env`, `settings.json`, `.claude/`, `.mcp.json` — в
   `.gitignore` и не в `git ls-files`. Скан дал только ложные срабатывания на
   слове «token» в `docs/paged.polyfill.js`. Скриншотов в репозитории нет.
   `test-areas/sample-transaction*.json` — синтетика, ПД нет.

6. **Режим разрешений** проставлен во всех 9 session-файлах (AUDIT-04 закрыт),
   бывшие плейсхолдеры `ВОПРОС` не осталось.

### Что осталось риском (вынесено автору, не правил сам)

- Аудит «другой моделью» (Opus/Codex/третья, слайд 26) — не проведён, только
  чистой сессией той же модели (`REPORT.md` §§ 7/10 честно фиксируют).
- Ветки `cards/fees/transfers` и worktree-каталоги не почищены, в `origin` не
  запушены — по клону видны только merge-коммиты (задокументировано).
- Изоляция субагентов от правки `app/` — инструкцией в промпте, не набором
  инструментов (задокументировано в обоих параллельных session-файлах).

## Использованные инструменты

| Инструмент | Действие | Зачем |
|---|---|---|
| PowerShell (`git log/show/status/config/rev-list`, `Test-Path`) | Инспекция истории, сверка `HEAD`↔`origin/main`, поиск Python/Chrome | Проверка воспроизводимости и актуальности артефактов |
| Bash (`git fetch`, `git grep`) | Синхронизация с remote, скан секретов | Требование чек-листа «секретов нет» |
| Read / Grep / Edit | Разбор и точечная правка правил, отчёта, журнала | Сверка согласованности и её восстановление |
| `uv` 0.12.7 → CPython 3.13.15 (из кэша, без установки в систему) | Запуск `docs/build-pdf.py` | Пересборка PDF при отсутствии системного Python |
| headless Chrome (`Program Files (x86)`) | `--print-to-pdf` внутри `build-pdf.py` | Рендер `test-plan.pdf` / `report.pdf` |

## Второй заход — по разрешению автора (промпты 2, 3)

### Кросс-проверка другой моделью (риск 1, слайд 26)

Запущены два субагента независимого аудита в фоне, каждому — полный набор
требований (`04`/`05` раздатка), правила проекта и весь репозиторий,
режим read-only:

- **Claude Opus** — аудит выполнен. 18 находок с градацией серьёзности + вердикт
  по обязательным пунктам и чек-листам + раздел «что сделано хорошо». Ключевое:
  все три обязательных пункта закрыты, следы параллельных агентов **реальные**;
  секретов нет; но `README.md`/`SPEC.md` не догнали последний коммит (писали, что
  `build-pdf.py` непереносим — уже неправда), `docs/report.pdf` рендерился до
  правки § 1 и содержит один устаревший абзац, три рассинхрона цифр в `REPORT.md`
  (§ 3 «один раз» vs дважды; § 9 «9/1/3» vs 9/2/3; § 9 «3 бага BUG-01…03» vs 4).
  Независимо подтвердил: хук `rtk` в этом окружении активен и **ломает**
  bash-команды (`rtk: command not found`) — то есть «хук технически исправен»
  верно лишь наполовину.
- **Fable 5** — упал на старте (`Fable 5 requires usage credits`). Codex в
  окружении недоступен. Итог: кросс-проверка — одна сильная сторонняя модель
  (Opus) вместо трёх; зафиксировано честно в `README.md` и здесь.

### Что поправлено по находкам Opus

- `README.md` — «Как запустить» шаг 2 (Playwright автором не прогонялся — прямо
  сказано, вместо «Ожидаемо:»), шаг 5 (сборщик переносим); раздел «Что не
  сделано»: `SPEC.md` согласована, режим разрешений проставлен везде,
  `build-pdf.py` переносим, кросс-проверка Opus проведена, формулировка про
  worktree-ветки смягчена (история воспроизводится через merge-коммиты).
- `REPORT.md` — § 3 «прогонялся дважды» (было «один раз»); § 9 «9/2/3 из 14»
  (было «9/1/3») и «4 подтверждённых бага BUG-01…04» (было «3, BUG-01…03»).
- `SPEC.md` — критерий `[ ] build-pdf.py пересобирает PDF` → `[x]` (подтверждено
  2026-08-29, скрипт переносим).
- `docs/build-pdf.py` — `AUTHOR` вынесен в `PDF_AUTHOR` с дефолтом (последний
  хардкод из AUDIT-08).
- **Пересборка PDF ещё раз** — после всех правок `REPORT.md`, чтобы `report.pdf`
  не содержал устаревший абзац § 1 (находка C Opus).
- Промпт 1 в этом файле переписан ближе к дословному (находка J Opus про
  пересказ вместо копии).

### Риск 2 (журнал доустановки инструментов)

Заведён `sessions/2026-08-29-tools-rtk-claude-mem.md` — восстановлен из коммитов
`30f1c1d`/`0ddb0dc`, `TOOLS.md` и `REPORT.md` § 7, с явной оговоркой в шапке, что
это ретроспективная запись (дословных промптов той работы не было). Opus отметил
(находка K), что по букве `AGENTS.md` такой журнал «не засчитывается» — оговорка
в шапке оставлена как единственный честный вариант, за живой не выдаётся.

### Риск 3 (worktree / баги / хардкод автора)

- Worktree/ветки — в этом клоне чистить нечего (проверено: `git branch -a`
  только `main`, `git worktree list` только основная копия, `git stash list`
  пуст). `STATE.md` и `README.md` уточнены.
- Хардкод автора в `build-pdf.py` — снят (`PDF_AUTHOR`).
- **BUG-02/03/04** — на этом заходе не чинились (`AGENTS.md`: правка `app/` =
  отдельная фикс-сессия). **Сделаны следующей сессией** по явному разрешению
  автора — `sessions/2026-08-29-fix-bug-02-03-04.md`.
- **Playwright** — на этом заходе не прогонялся. **Установлен в репозиторий и
  прогнан** следующей сессией; далее туда же добавлены seed-тесты
  APP-008/011/012 — итог **9/9 PASS**.

### Третий заход — аудит сторонней моделью Qwen (риск 1, слайд 26)

Промпт для «тайной» кросс-проверки написан по просьбе автора; автор прогнал его
в **Qwen** по публичному репозиторию (без локального контекста) и вернул отчёт.
Так набралась третья независимая модель: Sonnet (чистая сессия) + Opus
(субагент) + Qwen (сторонняя) — рекомендация слайда 26 закрыта (Codex в
окружении по-прежнему недоступен).

Находки Qwen (высокая/средняя) и что сделано:

- **E1 (высокая) — `README.md` шаг 2 «Playwright автором НЕ выполнялся»
  противоречит последнему коммиту** (`d9489f9`, прогон 6/6). Правда: строку
  писали до фикс-сессии, после прогона забыли обновить. → Шаг 2 переписан
  («выполнен 2026-08-29, 6/6 PASS»); заодно приведены к факту разделы «Что
  сделано» (все баги починены, автотест зелёный, аудит тремя моделями) и
  «Что не сделано» README, и абзац «Метод».
- **E4 / Г1 — PDF отстают от HEAD на один коммит** (не содержат фиксов
  BUG-02/03/04). → PDF пересобраны ещё раз, после всех правок README/REPORT.
- **E8 (низкая) — `build-pdf.py` ищет Chrome только в Windows-путях.** →
  Добавлены кандидаты macOS (`/Applications/Google Chrome.app/...`) и Linux
  (`/usr/bin/google-chrome`, `chromium`, …) + `shutil.which`.
- **E6 (низкая) — локальные пути с именем пользователя в session-файлах.** Оба
  внешних аудитора отметили как «низкая важность». Сначала оставил как есть
  (правка уже закоммиченного журнала). **Затем — по прямой просьбе автора**
  (сообщение «обезличивание путей в sessions/ … Запускай оба») — выполнена
  механическая замена: `C:\Users\<имя>\` → `C:\Users\<user>\`,
  `C:\Users\Tester_QA\` и `D:\…\ДЗ 2\` → плейсхолдеры. Заменены только
  сегменты пути с именем пользователя; технический смысл записей (что и куда
  ставилось, баг с обратными слэшами в хук-команде) сохранён дословно.
- **E2, E3, E5, E7, E9–E15** — уже известные и честно зафиксированные в репо
  ограничения (изоляция инструкцией, логи инъекции не сохранены, SK-01…55
  одним коммитом, `paged.polyfill.js`, устаревший прогон 27-08, CRD-029,
  ретро-журнал инструментов, две модели вместо трёх — теперь три). Qwen сам
  помечает большинство как «не действие / честно зафиксировано». Правок не
  требуют.

Qwen отдельно подтвердил (независимо от Opus и Sonnet): обязательные 3 пункта
закрыты доказательствами; секретов нет; следы параллельных агентов реальные;
изоляция песочницы — «замеры, а не декларация»; честность отчётности —
«образцовая». Полный текст ответа Qwen — у автора (в `sessions/` не сохранён,
как и логи субсессий инъекции — то же ограничение).

## Использованные инструменты

| Инструмент | Действие | Зачем |
|---|---|---|
| PowerShell (`git log/show/status/config/rev-list`, `Test-Path`) | Инспекция истории, сверка `HEAD`↔`origin/main`, поиск Python/Chrome | Проверка воспроизводимости и актуальности артефактов |
| Bash (`git fetch`, `git grep`) | Синхронизация с remote, скан секретов | Требование чек-листа «секретов нет» |
| Read / Grep / Edit / Write | Разбор и точечная правка правил, отчёта, журнала | Сверка согласованности и её восстановление |
| Agent × 2 (subagent_type general-purpose, model opus / fable) | Независимый аудит другой моделью | Кросс-проверка, слайд 26 (Opus — ок, Fable — упал на кредитах) |
| `uv` 0.12.7 → CPython 3.13.15 (из кэша, без установки в систему) | Запуск `docs/build-pdf.py` (дважды) | Пересборка PDF при отсутствии системного Python |
| headless Chrome (`Program Files (x86)`) | `--print-to-pdf` внутри `build-pdf.py` | Рендер `test-plan.pdf` / `report.pdf` |

## Изменения в проекте

- `REPORT.md` — § 1 (сведение плана закоммичено; PDF пересобраны; `build-pdf.py`
  переносим), § 3 («дважды»), § 9 («9/2/3», «BUG-01…04»).
- `README.md` — «Как запустить» шаги 2 и 5; раздел «Что не сделано» — 5 устаревших
  пунктов приведены к факту.
- `SPEC.md` — критерий приёмки по `build-pdf.py` → `[x]`.
- `sessions/STATE.md` — шапка на эту сессию; «Известные проблемы» и «Следующий
  шаг» переписаны под актуальное состояние (PDF, `build-pdf.py`, worktree,
  session-файл инструментов); worktree-пункт уточнён.
- `workflows/qa-regression-workflow.md` — «Статус»: счёт прогона 2026-08-28 → 9/2/3.
- `docs/build-pdf.py` — `_find_chrome()` (Chrome→Edge, `CHROME_BIN`), `STAGE`
  через `tempfile`, `AUTHOR` через `PDF_AUTHOR`, `DATE_HUMAN` → 29 августа,
  строки «Краткое резюме» обоих документов приведены к текущим `REPORT.md`/
  `test-plan.md`.
- `docs/test-plan.pdf`, `docs/report.pdf`, `docs/test-plan.html`,
  `docs/report.html` — пересобраны (дважды: после правок § 1 и после правок § 3/§ 9).
- `sessions/2026-08-29-tools-rtk-claude-mem.md` — новый (ретроспективный журнал).
- `sessions/2026-08-29-course-compliance-audit.md` — этот файл.
- Код `app/`, `test-plan.md`, `test-areas/`, баг-репорты — не трогались.

## Финальный вердикт

Все три обязательных пункта и оба чек-листа сдачи закрыты; секретов нет;
внутренняя согласованность README/REPORT/SPEC/STATE/TOOLS/git восстановлена
(в т.ч. по находкам независимого аудита на Opus). Что доделано в эту сессию:
отмечено закоммиченное сведение плана; счёт регрессии и число багов
синхронизированы во всех файлах; печатные PDF дважды пересобраны из актуального
Markdown; `docs/build-pdf.py` сделан переносимым (Chrome-детект, temp, `PDF_AUTHOR`)
— AUDIT-08 практически снят; заведён недостающий session-файл на доустановку
`rtk`/`claude-mem`; проведена кросс-проверка сильной сторонней моделью (Opus).

Остаточные риски, вынесенные автору: (1) кросс-проверка — одна модель вместо
трёх (Fable — нет кредитов, Codex — нет в окружении); (2) BUG-02/03/04 не
починены — по правилу проекта это отдельные фикс-сессии; (3) `npx playwright
test` в настоящем Playwright не прогонялся — эквивалент отыгран браузерным
агентом, формулировки в `README.md` приведены к честным.
