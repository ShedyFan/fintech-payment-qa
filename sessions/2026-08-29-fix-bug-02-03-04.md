# Сессия — фикс BUG-02 / BUG-03 / BUG-04

- **Дата:** 2026-08-29
- **Ассистент:** Claude Code (Agent SDK), модель `claude-sonnet-5`
- **Режим разрешений:** обычные подтверждения (не `bypassPermissions`, не
  `/sandbox`). Правка кода продукта `app/` требует явного контроля человека —
  это тот случай из Части I `AGENTS.md`; постановка и разрешение даны автором
  явно (см. «Промпты»).
- **Статус:** завершена

Отдельная сессия «фикс» по правилу проекта («правка продукта — отдельная сессия
«фикс», с явной постановкой»; `AGENTS.md` → «Что нельзя трогать»). Три бага
чинятся вместе, потому что зоны не пересекаются (три независимые функции:
`todayKey`, `parseAmount`, `loadState`), а готовые диагноз и точный фикс на
каждый уже подготовлены параллельным разбором —
`sessions/2026-08-29-parallel-bug-analysis.md`, «Приложение. Диагностика и
предложенные фиксы».

## Промпты

### Промпт 1

> запускай фиксы

(в том же сообщении, отдельными пунктами: «Установи и запусти» — про Playwright;
«Проверено, публичный» — про репозиторий. Полный контекст — сессия
`sessions/2026-08-29-course-compliance-audit.md`, где эти три бага и прогон
Playwright перечислены как остаточные риски, вынесенные автору.)

## Постановка (что и почему чиним)

### BUG-02 — дневной лимит по дате UTC, а не по местной

`todayKey()` = `new Date().toISOString().slice(0,10)` — `toISOString()` всегда
UTC. В UTC+3 обнуление дневного счётчика происходит в 03:00 местного времени,
ночные переводы попадают во «вчерашний» бюджет лимита.
**Решение:** ключ суток по местному времени устройства
(`getFullYear`/`getMonth`/`getDate`). Формат `YYYY-MM-DD` не меняется —
совместим с уже сохранёнными `op.day`.
**Продуктовое решение (за автором, зафиксировано):** сутки лимита считаются по
**местному времени устройства пользователя**. Для учебного демо-стенда без
сервера это допустимо; в «настоящем» продукте клиентские часы недоверенные и
обычно берут серверный/регламентный пояс.

### BUG-03 — разделитель разрядов молча усекает сумму (`1 000` → 1 ₽)

`parseAmount` = `parseFloat(String(value).replace(",", "."))`. `parseFloat`
разбирает читаемый префикс и молча отбрасывает хвост; `isFinite(amount) &&
amount > 0` видит только итог и не отличает «100» от «100, из которого
отброшено „abc“».
**Решение:** строгая regex-валидация всей строки
(`/^-?\d+([.,]\d{1,2})?$/`), иначе `NaN`. В платёжном виджете молчаливое
усечение опаснее явного отказа (списание необратимо, отказ — нет). Поддержка
пробела как разделителя не снимает двусмысленность (`1.234,56` читается двояко)
— выбран строгий отказ. В `doTransfer` — отдельное сообщение о формате до
проверки «больше нуля»; пустой ввод по-прежнему даёт «Сумма должна быть больше
нуля» (сообщение APP-005 не меняется). Знак минуса в регулярке сохранён, иначе
для `-5` сменилось бы сообщение (регрессия APP-005).

### BUG-04 — валидный JSON с неверной схемой роняет историю

`loadState()` проверяет только `typeof parsed.balance === "number"` и
`Array.isArray(parsed.operations)` — форма элементов не проверяется, `{}`
проходит; `renderHistory()` падает на `op.amount.toFixed` каждую секунду
(`setInterval`). Нарушение `SPEC.md` («сброс к начальному, ошибка в консоль не
выбрасывается»).
**Решение:** функция `isValidOperation(op)` (наличие и типы `id`, `recipient`,
`amount`, `fee`, `status ∈ {done, cancelled}`, `createdAt`, `day`); в
`loadState()` — цикл по `operations`, при первом невалидном элементе
`return defaultState()` (**полная отбраковка**, не построчная фильтрация:
`balance` хранится отдельно и не пересчитывается из `operations`, частичная
фильтрация дала бы ложное чувство целостности и ослабила бы `spentToday()` как
бизнес-инвариант — `SPEC.md` описывает контракт как двоичный сброс). Заодно
`isFinite(parsed.balance)` (`typeof NaN === "number"` проходил старую проверку).
Второй барьер — `if (!isValidOperation(op)) continue;` в самом `renderHistory()`.
**За автором (архитектурное):** включать ли `id` в обязательную схему (баг-репорт
не упоминал, включён по факту использования в `cancelOperation`).

## Размышления

- **Почему три бага в одной сессии, а не в трёх.** Правило проекта требует
  отдельную сессию на *правку продукта*, а не на каждый баг. BUG-01 чинился
  один — там фикс затрагивал две функции (`calcFee`, баланс в `doTransfer` и
  `cancelOperation`) и продуктовое решение (режим округления). Здесь три фикса
  строго локальны и не пересекаются; объединение не размывает след работы, а
  постановка на каждый — явная и отдельная (выше).
- **BUG-01 «Задело» закрывается заодно.** Запись в `bugs/BUG-01` отмечала: пока
  сумма с >2 знаками принимается без нормализации, `round2(balance)` может
  разойтись с сохранённым `amount` в 3-м знаке. Regex BUG-03 (`\d{1,2}` после
  разделителя) физически не пропускает такие суммы — дыра закрыта.
- **Экспоненциальная запись (`1e3`)** — открытый вопрос №8 `SPEC.md` и пункт в
  `test-areas/app.md`. Новая грамматика её тоже отклоняет — вопрос закрывается
  побочно.
- **Playwright.** Тесты `tests/payments.spec.js` были заранее написаны так,
  чтобы позеленеть после фикса: APP-009 — ветка `else` (`operations.length === 0`
  → `#msg` не пуст), APP-014 — `.empty` видно, баланс 50000, нет `pageerror`.
  После фикса обе должны стать зелёными без правки ассертов — только шапка-
  комментарий и `tests/README.md`.

## Использованные инструменты

| Инструмент | Действие | Зачем |
|---|---|---|
| Edit | Правка `app/index.html` (3 функции + `doTransfer` + `renderHistory`) | Собственно фиксы |
| `npm` / `@playwright/test` / `npx playwright install chromium` | Установка Playwright в репозиторий | Прогон `tests/payments.spec.js` в настоящем Playwright (риск №3) |
| `python -m http.server 8765` | Поднять `app/` под тесты | Стенд для Playwright |
| `npx playwright test` | Прогон всех 6 тестов | Доказательство фиксов запуском с выводом |

## Изменения в проекте

- `app/index.html` — `todayKey()` (BUG-02); `AMOUNT_RE` + `parseAmount()` и
  проверка формата в `doTransfer()` (BUG-03); `isValidOperation()` +
  валидация в `loadState()` + барьер в `renderHistory()` (BUG-04). Разметка и
  стили не тронуты.
- `bugs/BUG-02…04*.md` — добавлен раздел «Исправлено» (как в BUG-01).
- `app/build-brief.md` — раздел про фиксы 2026-08-29 (часовой пояс суток,
  формат ввода суммы, валидация схемы `localStorage`).
- `tests/payments.spec.js` — шапка-комментарий: APP-009/APP-014 теперь зелёные
  (регрессионная страховка), как APP-003.
- `tests/README.md` — таблица ожидаемого результата приведена к «всё PASS»,
  добавлен реальный вывод прогона.
- `test-areas/transfers.md`, `test-areas/fees.md`, `test-plan.md`,
  `SPEC.md` — точечные пометки, что открытые вопросы по часовому поясу /
  формату суммы / битой схеме закрыты фиксами.
- `package.json`, `package-lock.json` — Playwright как dev-зависимость
  (`node_modules/` в `.gitignore`).
- `sessions/STATE.md`, `sessions/TOOLS.md`, `REPORT.md` — обновлены.
- `sessions/2026-08-29-fix-bug-02-03-04.md` — этот файл.

## Прогон Playwright

Установлено в репозиторий (риск №3 закрыт): `@playwright/test` 1.62.1
(`npm i -D`), Chromium headless-shell 151.0.7922.34
(`npx playwright install chromium`). Node 24.19.0. `package.json` очищен от
мусора `npm init -y` (в `description` попал кракозябренный кусок README),
добавлен `scripts.test`. `.gitignore` — `test-results/`, `playwright-report/`,
`.last-run.json`.

Продукт поднят `python -m http.server 8765` (интерпретатор — CPython 3.13 из
кэша `uv`), смоук `GET /app/index.html` → 200. Прогон:

```
Running 6 tests using 1 worker
  ok 1 tests\payments.spec.js:41:1 › APP-001 перевод 1000 — баланс и история (512ms)
  ok 2 tests\payments.spec.js:47:1 › APP-005 нулевая сумма отклонена (181ms)
  ok 3 tests\payments.spec.js:54:1 › APP-007 нехватка средств с учётом комиссии (168ms)
  ok 4 tests\payments.spec.js:61:1 › APP-003 [BUG-01 fixed] комиссия округляется, баланс без долей копейки (192ms)
  ok 5 tests\payments.spec.js:71:1 › APP-009 [BUG-03] разделитель разрядов в сумме (165ms)
  ok 6 tests\payments.spec.js:81:1 › APP-014 [BUG-04] валидный JSON с неверной схемой не роняет экран (1.7s)

  6 passed (4.1s)
```

`APP-009` и `APP-014` были красными до этой сессии — стали зелёными. Регрессии
по APP-001/003/005/007 нет.

Дополнительная проверка regex суммы (node, вне Playwright): `1000`/`1000.50`/
`1000,50` → принято; `1 000`/`1,000`/`1.234,56`/`100abc`/`1.2.3`/`1e3`/
`1000.567` → отклонено; `-5`/`0` → проходят regex, ловятся проверкой «> 0»
(сообщение APP-005 не изменилось); пустой ввод → «Сумма должна быть больше
нуля» (не изменилось).

`node --check` по извлечённому `<script>` — синтаксис OK.

## Что не покрыто

- **BUG-02** — граница суток дневного лимита автотестом не проверяется (нужен
  контроль системного времени / seed `operations[].day`); фикс подтверждён
  чтением кода и тем, что `todayKey()` теперь строит ключ из локальных
  `getFullYear/getMonth/getDate`. Ручной прогon APP-008 с seed — за автором.
- **APP-011** (окно 60 с) и **APP-012** (полная перезагрузка + консоль) —
  как и раньше, требуют ручного прогона; в Playwright APP-014 частично их
  трогает (перезагрузка + отсутствие `pageerror`).
- Продуктовое решение по BUG-02 (местное время устройства) — принято в этой
  сессии за автора как владельца продукта; при желании можно пересмотреть в
  пользу серверного/регламентного пояса.

## Финальный вердикт

**BUG-02, BUG-03, BUG-04 — исправлены** в `app/index.html` тремя локальными
правками; регрессия закрыта прогоном `tests/payments.spec.js` в настоящем
Playwright (**6/6 PASS**), `APP-009` и `APP-014` из красных стали зелёными.
Дефекты в `bugs/` помечены разделом «Исправлено», решения зафиксированы в
`app/build-brief.md`. Открытых красных тестов на продукте не осталось.

Осталось (за автором): ручной прогон APP-008 (seed, граница дневного лимита),
APP-011 (таймер 60 с), APP-012 (перезагрузка + DevTools-консоль); при
желании — пересмотр продуктового решения по часовому поясу лимита.
