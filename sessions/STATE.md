# Состояние проекта

**Обновлено:** 2026-08-29, сессия «аудит соответствия требованиям курса перед
сдачей» (`sessions/2026-08-29-course-compliance-audit.md`)

## Готово

- **Система под тестом `app/index.html`** — платёжный кабинет (баланс, перевод,
  комиссия 1% с клиппингом 10/300, дневной лимит 100 000, отмена в окне 60 сек,
  история, `localStorage`). Собран за один проход по `app/build-brief.md`.
  Поднимается на `http://localhost:8765/app/index.html`, консоль на чистом старте
  без ошибок. Дизайн переработан отдельным проходом (тёмная тема), логика не
  тронута; снимок «до» — `app/index.before-design.html`.
- **`CLAUDE.md` / `AGENTS.md`** — Часть I дословно, Часть II заполнена под проект
  (черновик, ждёт вычитки автором).
- **`SPEC.md`** — спецификация **согласована** (2026-08-28): критерии приёмки
  проставлены; блоки `ВОПРОС:` закрыты (часть разрешена, часть оставлена
  открытой осознанно — см. раздел «Открытые вопросы» спеки).
- **Тест-план `test-plan.md`** — Области 1–3 (TR-001…044, CRD-001…044,
  FEE-001…048) с открытыми вопросами; **Область 4 — APP-001…014** (исполняемые
  против `app/`); SK-01…55; чек-лист «Граничные значения в деньгах».
- **Баг-репорты** `bugs/BUG-01…04` (формат из `AGENTS.md`, с доказательствами).
  BUG-01 (округление комиссии) **починен** — `sessions/2026-08-28-fix-bug-01.md`.
  BUG-02 (лимит по UTC), BUG-03 (парсинг суммы), BUG-04 (битая схема в
  `localStorage` роняет историю) — открыты.
- **Автотест** `tests/payments.spec.js` (Playwright): зелёные на базовом
  поведении + регрессия по BUG-01; красные APP-009 (BUG-03), APP-014 (BUG-04).
  `storedState()` сделан null-safe (AUDIT-07). Прогон без Playwright — браузерным
  агентом, вывод в `sessions/`.
- **Наивный прогон** `naive-run/test-plan-naive.md` (субагент, чистый контекст) +
  «## 9. Сравнение наивного и спекового прогонов» в `REPORT.md`.
- **Workflow регрессии** — два прогона: `sessions/2026-08-27-regression.md`
  (95 параметрических, проверка готовности) и `sessions/2026-08-28-regression.md`
  (Область 4 против `app/`, с дополнением после аудита — счёт 9/2/3 из 14).
  `Статус` в workflow обновлён.
- **Изоляция песочницы** — `REPORT.md` § 5. **Инъекция** — `REPORT.md` § 6
  (3 вектора × Haiku 4.5 / Sonnet-Opus).
- **Агенты параллельно** — `REPORT.md` § 2 (обновление 2026-08-29):
  `sessions/2026-08-29-parallel-bug-analysis.md`, три субагента `general-purpose`
  одним батчем, каждый на свою зону (диагностика BUG-02/03/04), без правки
  `app/`. Результаты — диагноз + предложенный фикс на каждый баг, сведены в
  один документ.
- **Замер параллельно/последовательно** — `REPORT.md` § 8 (2026-08-29,
  `sessions/2026-08-29-benchmark-and-tools.md`): та же задача (приоритизация
  открытых вопросов TR/CRD/FEE) прогнана и тремя субагентами, и основной
  сессией без субагентов. Числовой замер не получился чистым (конкурентная
  нагрузка на сессию во время параллельной части), но найдена содержательная
  находка — последовательный прогон точнее оценил TR-005/031, перечитав код
  `app/index.html`, чего параллельный субагент не сделал.
- **Оптимизатор контекста / claude-mem — оба доведены до конца и проверены
  вживую (2026-08-29).** `claude-mem`: MCP `mcp-search` подключён (`✓
  connected · 14 tools`), сессия пишет наблюдения. `rtk`: найден и исправлен
  баг в JSON-команде хука (обратные слэши съедались bash — фикс на прямые
  слэши), после фикса `rtk gain` показал реальные **3 команды / 86.4%
  экономии токенов** на `git status`. Открытый вопрос: сам `rtk`
  по-прежнему считает хук «не установленным» — экономия шла через ручную
  маршрутизацию по инструкции `RTK.md`, а не через автоматический перехват
  хуком; причина расхождения не докопана. Подробности — `REPORT.md` § 7,
  `sessions/TOOLS.md`.
- **Независимый аудит** всего репозитория: чистой сессией той же модели —
  `sessions/2026-08-28-independent-audit.md` (14 находок + построчный вердикт);
  **другой моделью (Claude Opus)** — субагентом 2026-08-29
  (`sessions/2026-08-29-course-compliance-audit.md`, раздел «Второй заход»):
  18 находок, все обязательные пункты подтверждены закрытыми, найденные
  рассинхроны README/REPORT/SPEC устранены. Codex/Fable в окружении недоступны.
- **`README.md`** переписан (AUDIT-02/03 + правки по аудиту Opus): «Как
  запустить», «Что не сделано и ограничения» (устаревшие пункты приведены к
  факту), убрано «параллельные агенты».
- **`REPORT.md`** — § 10 «Расхождения: где „готово“ не подтвердилось»; §§ 1/3/9
  синхронизированы 2026-08-29.
- Печатные версии `docs/test-plan.pdf`, `docs/report.pdf` — пересобраны
  2026-08-29 из актуального Markdown; `docs/build-pdf.py` сделан переносимым.

## В работе

- Ничего не в промежуточном состоянии. Открытые пункты — ниже.

## Не начато / за автором

- ~~Ответы на `ВОПРОС:` в `SPEC.md`~~ — закрыто 2026-08-28: потребитель —
  ревьюер курса; режим разрешений — разный по сессиям, восстановлен по логам;
  superpowers — подключён; продуктовые вопросы (часовой пояс, длина получателя,
  двойная отправка) оставлены открытыми осознанно.
- ~~Режим разрешений в session-файлах~~ — проставлен во всех 6 (AUDIT-04 закрыт).
- ~~**Аудит другой моделью**~~ — проведён Claude Opus (субагент, 2026-08-29).
  Три модели (Opus + Codex + третья, слайд 26) — не вышло: Codex недоступен,
  Fable 5 без кредитов. Полноценная тройная кросс-проверка — за автором при
  наличии доступа.
- Фикс-сессии по **BUG-02, BUG-03, BUG-04** — диагностика и предложенный фикс
  для каждого готовы (`sessions/2026-08-29-parallel-bug-analysis.md`), сама
  правка `app/` ещё не сделана (правило проекта — отдельная сессия «фикс», как
  для BUG-01); ручной прогон **APP-008** (seed), **APP-011** (таймер 60 с),
  **APP-012 / APP-014** (перезагрузка страницы).
- Прогон `tests/payments.spec.js` в настоящем Playwright (Node/npm в окружении
  сборки нет).

## Активные инструменты

Claude Code (песочница, не bypass); git worktree + ветки по областям (локально,
не в remote); субагент `general-purpose` (проверка полноты; наивный прогон;
параллельный батч из трёх — диагностика BUG-02/03/04, 2026-08-29);
`docs/build-pdf.py` + headless Chrome + Paged.js. Подробности и тупики —
`sessions/TOOLS.md`.

## Инструменты — отложено

- **Pencil (pen.dev)** — не подключён (редакторное расширение + авторизация, из
  агентной сессии не поднять). Дизайн-проход по `app/` сделан вручную.

## Известные проблемы

- **`docs/build-pdf.py`** (AUDIT-08): 2026-08-29 сделан переносимым — путь к
  Chromium ищется по списку (Chrome → Edge, переопределяется `CHROME_BIN`),
  временная папка — системный temp, автор титула — из `PDF_AUTHOR` с дефолтом.
  Запускается любым Python 3 со stdlib (стороннего `markdown` не требует).
  AUDIT-08 практически снят.
- **`docs/paged.polyfill.js`** (~921 КБ) в репозитории — намеренно, но раздувает
  клон (AUDIT-14).
- Ветки `cards`/`fees`/`transfers` и каталоги worktree после merge не удалены
  **на исходной машине автора**; в `origin` они не отражены. Свежий клон
  (в т.ч. `F:\AI\fintech-payment-qa`) их не содержит: `git branch -a` — только
  `main`, `git worktree list` — только основная копия, `git stash list` пуст.
  По репозиторию видны лишь три merge-коммита — это ожидаемо, чистить в
  публичной версии нечего.
- Прогон регрессии 2026-08-27 устарел (95 сценариев; области позже расширены,
  повторно не гонялись); шаг 4 регрессии 2026-08-28 — самопроверка без отдельного
  агента.
- `CRD-029` — дефектный сценарий (ожидаемый результат без проверяемого оракула),
  не переписан.
- `.gitignore` правился дважды из-за `/dev/null`-заглушек конфигов в песочнице.
- Логи субсессий теста инъекции (Haiku/Opus) в `sessions/` не сохранены.
- **Печатные PDF пересобраны 2026-08-29** (`docs/build-pdf.py`, Python 3.13
  через `uv`, рендер headless Chrome): `docs/test-plan.pdf` теперь с Областью 4
  (APP-001…014), `docs/report.pdf` — все десять разделов + «Честные тупики» +
  «Инструменты». Прежняя версия (`df3a3de`) отставала. Детали — `REPORT.md` § 1.
- ~~Нет отдельного session-файла на доустановку `rtk`/`claude-mem`~~ — заведён
  2026-08-29: `sessions/2026-08-29-tools-rtk-claude-mem.md` (восстановлен из
  коммитов `30f1c1d`/`0ddb0dc`, `TOOLS.md`, `REPORT.md` § 7, с явной оговоркой
  о реконструкции).

## Следующий шаг

Все обязательные пункты закрыты по содержанию и запушены в `origin/main`;
печатные PDF пересобраны и актуальны.

Дальше — по желанию: аудит другой моделью, фикс-сессии по BUG-02/03/04 (готовые
диагноз+фикс — в `sessions/2026-08-29-parallel-bug-analysis.md`, план — через
`superpowers:writing-plans`), ручной прогон APP-008/011/012/014.

Все обязательные и большинство опциональных пунктов задания закрыты, включая
живую проверку `rtk`/`claude-mem` (см. выше). Открытый вопрос на будущее (не
блокирует сдачу): почему `rtk` не засчитывает свой же технически исправный
хук как «установленный» — см. `sessions/TOOLS.md`.
