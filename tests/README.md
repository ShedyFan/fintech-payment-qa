# tests — автотест продукта `app/`

`payments.spec.js` — Playwright-сценарий против `app/index.html`.

## Как запустить

```bash
# 1. поднять продукт
python -m http.server 8765

# 2. в отдельном терминале, из корня репозитория
npm install                 # ставит @playwright/test (см. package.json)
npx playwright install chromium
npm test                    # = npx playwright test tests/payments.spec.js
```

## Прогон 2026-08-29 (настоящий Playwright)

Окружение: Node 24.19.0, `@playwright/test` 1.62.1, Chromium (headless-shell)
151.0.7922.34. Продукт поднят `http.server 8765`. Сессия —
`sessions/2026-08-29-fix-bug-02-03-04.md`.

```
> playwright test tests/payments.spec.js

Running 6 tests using 1 worker
  ok 1 APP-001 перевод 1000 — баланс и история
  ok 2 APP-005 нулевая сумма отклонена
  ok 3 APP-007 нехватка средств с учётом комиссии
  ok 4 APP-003 [BUG-01 fixed] комиссия округляется, баланс без долей копейки
  ok 5 APP-009 [BUG-03] разделитель разрядов в сумме
  ok 6 APP-014 [BUG-04] валидный JSON с неверной схемой не роняет экран

  6 passed (3.2s)
```

| Тест | Результат | Комментарий |
| --- | --- | --- |
| APP-001 перевод 1000 | PASS | базовый сценарий |
| APP-005 нулевая сумма | PASS | валидация суммы; `storedState` null-safe (AUDIT-07) |
| APP-007 нехватка средств | PASS | проверка баланса |
| APP-003 [BUG-01 fixed] округление комиссии | PASS (было FAIL до 2026-08-28) | `fee = 12.35`, `balance = 48753.09` — `sessions/2026-08-28-fix-bug-01.md` |
| APP-009 [BUG-03] разделитель разрядов | **PASS (было FAIL до 2026-08-29)** | `1 000` отклоняется с сообщением о формате, операция не создаётся — `sessions/2026-08-29-fix-bug-02-03-04.md` |
| APP-014 [BUG-04] битая схема в localStorage | **PASS (было FAIL до 2026-08-29)** | `{"balance":0,"operations":[{}]}` → сброс к начальному, экран не падает, консоль чистая |

Красных тестов на текущей версии `app/` нет — после фиксов BUG-01/03/04 все
шесть зелёные. APP-001/005/007 — регрессионная страховка базового поведения,
APP-003/009/014 — регрессия по трём фикс-сессиям.

BUG-02 (часовой пояс дневного лимита) исправлен в коде 2026-08-29, но
отдельного автотеста на границу суток нет — нужен контроль системного времени;
проверяется ручным прогоном с seed-состоянием (APP-008).

`storedState()` в спеке сделан null-safe (AUDIT-07) — прежнее «Замечание по
спеке» про `TypeError` в `APP-005` закрыто.

## Прогон без Playwright

Проверки также воспроизводились через браузерный агент:
- первичный прогон (до фиксов) — `sessions/2026-08-28-app-testing.md`;
- после фикса BUG-01 — `sessions/2026-08-28-fix-bug-01.md`;
- после фиксов BUG-02/03/04 — прогон в настоящем Playwright (выше).
