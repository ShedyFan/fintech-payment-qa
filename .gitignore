.env
settings.json
.claude/settings.local.json
*.log
node_modules/
# Playwright: артефакты прогонов и служебное — в репозиторий не идут
test-results/
playwright-report/
blob-report/
.last-run.json
docs/*.pdf.tmp
.claude/
.mcp.json

# Файлы конфигурации, которые песочница Claude Code подменяет /dev/null-заглушками
# (character special, 0 байт) — не regular files, из-за них падал `git add -A`.
.bash_profile
.bashrc
.profile
.zprofile
.zshrc
.gitconfig
.gitmodules
.ripgreprc
.idea
.vscode
